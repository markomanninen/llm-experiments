{
  "description": "Retrieve data entry/entries by key or key_group name corresponding given value. For instance, find user profile with field=key_group and value=user_profile.",
  "arguments": {
    "type": "object",
    "properties": {
      "field": {
        "type": "string",
        "description": "Data field name, key or key_group.",
        "enum": ["key", "key_group"]
      },
      "value": {
        "type": "string",
        "description": "Data field value"
      }
    },
    "required": ["field", "value"],
    "additionalProperties": false
  }
}